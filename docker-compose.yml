services:
  querylens:
    build: .
    container_name: querylens
    ports:
      - "8501:8501"
    volumes:
      # Mount the dataset so swapping the CSV doesn't require a rebuild
      - ./data:/app/data:ro
    environment:
      - PYTHONUNBUFFERED=1
    restart: unless-stopped
